// ============================================================================
// 설정 도메인 타입
// ============================================================================
//
// 사용자가 고르는 앱 설정값. 실제 동작에 반영되는 것만 둔다(테마 모드·기본 분석 범위).
// StatsRange는 StatsDetail의 범위 세그먼트와 같은 값이라 여기서 공유한다
// ============================================================================

// 테마 모드 — 시스템 추종 / 항상 라이트 / 항상 다크
export type ThemeMode = 'system' | 'light' | 'dark';

// 통계 분석 범위 — 최근 100회 / 30회 / 전체 (StatsDetail 세그먼트와 동일)
export type StatsRange = '100' | '30' | 'all';
